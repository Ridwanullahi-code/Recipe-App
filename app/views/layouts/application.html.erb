<!DOCTYPE html>
<html>
  <head>
    <title>RecipeApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <%= javascript_include_tag "application", "index", "data-turbo-track": "reload" %>
  </head>

  <body>
  <header> <%= render partial: "shared/navbar" %></header>
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>
    <%= yield %>
    <footer>
      <div>@Aman Worku</div>
       <div>@Ade179</div>
        <div>@Ajayi ridwan</div>

    </footer>

  </body>

</html>
