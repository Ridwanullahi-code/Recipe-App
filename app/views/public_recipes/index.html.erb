<div class="">
  <h1 class="">All Recipes</h1>
  <% if @recipes.empty? %>
    <p class="">No public recipes.</p>
  <% else %>
    <div class="">
  <div class="">
    <%= link_to public_recipe.name, recipe_path(public_recipe.id), class: "" %>
    <h3 class=""> <%= public_recipe.user.name %> </h3>
  </div>
  <div class="">
    <p class="">Total food items: <%= count_items(public_recipe.foods)%> </p>
    <p class="">Total food items: $<%= total_price(public_recipe)%> </p>
  </div>
</div>
  <% end %>
</div>
